<div>
    <div>Test</div>
    {{#each beersFromApi}}
        <div>
            <img src="{{this.image_url}}" alt="{{this.name}}">
            <h1>{{this.name}}</h1>
            <p>{{this.description}}</p>
            <h3>{{this.tagline}}</h3>
        </div>
    {{/each}}
</div>